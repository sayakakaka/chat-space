.chat-side
  .side-header
    .side-header__box
      %h3.side-header__box__name
        = current_user.name
      %ul.side-header__box__menu
        %li.side-header__box__edit
          = link_to new_group_path do
            = icon('far','edit', class: 'icon')
        %li.side-header__box__new
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
  .groups
    - current_user.groups.each do |groups__new|
      .groups__new
        = link_to '#' do
          %p.groups__new__group-name
            = groups__new.name
          %p.groups__new__group-messages
            まだメッセージがありません